# Client environment variables
# Copy this file to client/.env and fill in the values

# API URL for the backend server
# In development, this can be left unset (defaults to http://localhost:5000)
# In production, set this to your deployed server URL, OR leave unset if
# the client is served from the same origin as the server (uses relative paths)
VITE_API_URL=http://localhost:5000