# ── LOCAL DEVELOPMENT ─────────────────────────────────────────
# Vite proxies /api/* → http://localhost:5000 automatically
# Do NOT set VITE_API_URL here — the proxy handles it
VITE_APP_NAME=Alex Store
VITE_API_URL=http://localhost:5000